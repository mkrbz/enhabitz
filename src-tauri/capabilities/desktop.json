{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Global shortcuts — no such concept on mobile, so the plugin itself is excluded from the Android/iOS dependency graph (see Cargo.toml)",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main"],
  "permissions": ["global-shortcut:default"]
}
